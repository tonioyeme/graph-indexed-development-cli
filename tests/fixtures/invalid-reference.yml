# Test fixture: Invalid node reference

nodes:
  ComponentA:
    type: Component
    description: "A component"

edges:
  # References a node that doesn't exist
  - { from: ComponentA, to: NonExistentNode, relation: depends_on }
