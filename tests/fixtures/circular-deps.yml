# Test fixture: Circular dependencies

nodes:
  ComponentA:
    type: Component
    description: "Component A"
    layer: application

  ComponentB:
    type: Component
    description: "Component B"
    layer: application

  ComponentC:
    type: Component
    description: "Component C"
    layer: application

edges:
  # Circular: A -> B -> C -> A
  - { from: ComponentA, to: ComponentB, relation: depends_on }
  - { from: ComponentB, to: ComponentC, relation: depends_on }
  - { from: ComponentC, to: ComponentA, relation: depends_on }
